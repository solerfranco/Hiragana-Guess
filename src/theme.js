import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#ded8b8",
    },
    secondary: {
      main: "#333333",
    }
  },
  width: {
    width: "100%",
  },
});

export default theme;
